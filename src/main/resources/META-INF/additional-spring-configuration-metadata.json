{
  "properties": [
    {
      "name": "model-service.url",
      "type": "java.lang.String",
      "description": "Python 模型推理服务地址，如 http://localhost:8090。不配置时自动降级为 V1.0 趋势外推。"
    },
    {
      "name": "model-service.timeout-ms",
      "type": "java.lang.Integer",
      "description": "模型服务 HTTP 请求超时（毫秒），默认 5000。",
      "defaultValue": 5000
    }
  ]
}
